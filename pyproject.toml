[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["random_walk_env"]

[project]
name = "random_walk_env"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "pre-commit",
]
